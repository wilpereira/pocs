let validators = {
    itemA: 'test-class-a',
    itemB: 'test-class-b',
    itemC: 'test-class-c',
}

for(let key in validators) {
    // console.log(validators[key])
    // let selector = document.querySelector(validators[key])
    // console.log(element[key] = selector)
    console.log(typeof validators[key])
}